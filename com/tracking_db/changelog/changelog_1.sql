-- liquibase formatted sql]

-- changeset nishith:db labels: db context: generate_db
--  comment: This is generate the db schema in your db.
CREATE SCHEMA `db_versioning` ;

-- rollback DROP DATABASE db_versioning
